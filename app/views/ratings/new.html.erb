<%= render 'partials/header' %>

<h1>New Rating</h1>

<%= form_for :rating, url: {action: :create} do |f| %>
  <p>
  <strong><%= @restaurant.name %></strong>
</p>

<p>
  <%= f.label "Stars" %><br>
  <%= f.select(:stars, options_for_select(1..5)) %>
</p>

<p>
  <%= f.label :notes %><br>
  <%= f.text_area :notes %>
</p>

<p>
  <%= f.submit %>
</p>
<% end %>