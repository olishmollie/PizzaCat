<h1>Ratings</h1>

<table>
  <%= render 'partials/rating_th' %>
  <% @ratings.each do |r| %>
    <tr>
      <td><%= r.restaurant.name %></td>
      <td><%= r.stars %></td>
      <td><%= r.notes %></td>
    </tr>
  <% end %>
</table>